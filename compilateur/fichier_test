int glb=55;

int ifact(int n );

int gi(int a, int b );

int fib(int n )
{
	if (n<2)
	{
		return n;
	}else 
	{
		return fib(n-1)+fib(n-2); 
	}
}

int affich(int n , int a)
{
	print(a);
	print(n);
}
main()
{ 	int a =1;
	int a=2;
	fib(3);
	affich(a,5);
}
